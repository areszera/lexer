#include <stdio.h>

#define PI 3.14159

struct sds {
    int len;
    int alloc;
    unsigned char flags;
    char buf[;
};

@Override
float area(int arg) {
    return PI * arg * arg;
}

void loop(int n) {
    while (n) {
        int i;
        for (i = 0; i < n; i++)
            switch (i % 5)
            case 0:
                printf("YES!!!";
                break;
            default:
                break;
            }
        }
        if (sizeof i > 0) {
            goto label;
        }
    }
label:
    n > 0.0.0 ? n = 1abc : n = -0001;
}

int main() {
    // Inline comment
    extern static char ch = 'abc';
    auto int integer = 123;
    int float_1 = 1.23;
    int float_2 = 123f;
    int _long = 123l;
    int _long_long_long_long_long_long_long_long_long_long_long_long_long_long_long_long_long_long = 123ll;
    int _unsigned = 123u;
    int oct = 0123;
    int hex = 0x1a;
    if (integer >= float_1 + float_2 && _long == _long_long) {
        _unsigned++;
    }
    /**
     * Block comment
     */
    double a = (double) area(integer);
    printf("\"area is %f\, a);
    return 0;
}
